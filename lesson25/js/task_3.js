// Написать функцию, которая принимает три отдельные
// цифры и превращает их в одно число. Например:
// цифры 1, 4, 9 превратятся в число 149.
function threeDigitNumver(x, y, z) {
  return x * 100 + y * 10 + z;
}
